# 昭臣打卡 App 开发计划

> 目标：为工人提供按项目、班组和班次打卡的移动端，并生成可追溯的个人月报和项目月报。
> 后端作为 `seahorizon-backgroud` 内的独立业务模块运行，接口使用 `/api/ck/**`，数据表统一使用 `ck_` 前缀。
> 手写需求转写、疑问及待提供资料统一记录在 [REQUIREMENTS.md](REQUIREMENTS.md)，开发时以两份文件共同作为依据。

## 一、产品与技术原则

1. App 使用 Flutter，保留 prod/staging 双 flavor 和 test/prod 环境。
2. 打卡账号独立存放在 `ck_user`，不复用采购 App 账号。
3. 原始打卡不可直接覆盖；补卡、作废和修正必须留痕。
4. 服务器时间是正式打卡时间，手机时间只作诊断信息。
5. App 上报定位，后端计算项目距离、围栏结果并校验人员分配。
6. 日报/月报是计算结果，可从原始打卡、班次规则和调整记录重新生成。
7. 加班规则可以后续完善，但第一版数据模型必须支持跨日和多个时间段。

## 二、总体架构

| 层 | 方案 |
|---|---|
| App | Flutter；按 feature 分模块；Provider 管理状态；Dio 访问接口 |
| 后端 | 新增 `com.seahorizon.checkin` 独立模块 |
| 接口 | `/api/ck/**`；旧拦截器排除该路径，由 `CkAuthInterceptor` 独立处理 |
| 鉴权 | JWT 增加 `app=checkin`；旧 App Token 不能访问打卡接口 |
| 数据库 | 同一 MySQL；表名统一 `ck_`；Flyway 从 V30 开始 |
| 图片 | 复用 OSS，但使用独立 `checkin/` 顶层目录；打卡照片按项目、工人、日期归档并与打卡记录关联 |

## 三、核心业务定义

### 角色与人员归属

- `supervisor`：项目主管，在同一个 App 内管理项目、班组、人员、排班、修正和报表。
- `worker`：查看本人安排、现场打卡、查看本人记录和月报。
- 工人保存当前所属项目、班组和班次，不设置分配有效期；主管调整时直接切换。
- 打卡记录保存打卡当时的项目、班组和班次快照，后续切换不能改变历史报表。
- 项目主管只能管理明确分配给自己的项目，所有管理接口都按主管—项目关系鉴权。
- 每个项目独立配置多种班次，工人直接放入当前项目、班组和具体班次。

### 班次与打卡

- 班次由若干检查点组成，不把“四次/两次”写死在接口代码中。
- 白班初始模板包含上午上/下班、下午上/下班四个检查点。
- 夜班模板允许跨自然日，并明确考勤归属日期。
- 跨日夜班固定归属班次开始当天，跨月时也按开始日期归属月份。
- 每次打卡保存定位、精度、客户端时间、服务器时间、照片和唯一请求号。
- 同一请求号重复提交不得生成重复打卡。
- 缺卡、迟到、早退、围栏外先形成异常状态，不能静默算作正常出勤。
- 异常原始记录保留，但负责主管确认或修正前不计入工时和工数。
- 围栏外允许提交，App 提交前必须强提醒，后台标记为待主管处理异常。

### 工数、修正与报表

- `ck_clock_record` 保存不可变原始事实。
- `ck_clock_adjustment` 保存补卡、作废、修正前后内容、原因和操作者。
- `ck_attendance_day` 保存每日计算结果、规则版本和计算状态。
- 月报支持草稿、确认、冻结；冻结后修正生成新版本。

## 四、数据模型草案

| 表 | 主要用途 |
|---|---|
| `ck_user` | 独立账号，supervisor/worker |
| `ck_project` | 项目、中心坐标、围栏半径、状态 |
| `ck_project_supervisor` | 主管负责项目关系和数据权限边界 |
| `ck_team` | 班组 |
| `ck_worker_assignment` | 工人的当前项目、班组和班次归属，不含有效期 |
| `ck_shift_template` | 班次模板、跨日规则和规则版本 |
| `ck_shift_checkpoint` | 检查点及允许时间窗口 |
| `ck_clock_record` | 不可变原始打卡及幂等请求号 |
| `ck_clock_adjustment` | 补卡、作废、修正审计记录 |
| `ck_attendance_day` | 工人×项目×考勤日计算结果 |
| `ck_report_snapshot` | 月报冻结版本和状态 |

## 五、开发阶段

### 阶段 0：规则与接口契约

- 确认临时换班、休息日、节假日、无排班打卡和跨项目规则。
- 围栏外允许打卡，但按异常处理，主管处理前不计工时和工数。
- 确认离线打卡是否允许及最长补传时间。
- 固定登录、项目识别、打卡、调整和月报接口格式。
- 为唯一约束和状态流转编写测试清单。

### 阶段 1：项目骨架（已完成）

- Flutter 项目、prod/staging flavor、ENV、adb reverse 8082。
- EnvConfig、DioClient、AuthProvider、登录页。
- 独立 applicationId 和 Android release 签名配置。

### 阶段 2：账号与鉴权（当前开发）

- Flyway 创建 `ck_user` 和初始 admin。
- 实现 `/api/ck/auth/login`、`/api/ck/auth/me`。
- JWT 增加 `app=checkin`，完成旧/新拦截器隔离。
- App 使用强类型登录响应，正确恢复和清理登录态。
- 后端和 App 增加鉴权自动化测试。

### 阶段 3：项目、班组、人员与排班

- 建立项目、班组和人员当前归属；主管可直接切换人员的项目、班组或班次。
- 建立可配置的白班、夜班、高温时段、加班规则、检查点和每日排班。
- 每个项目独立维护多种班次，并把人员分配到具体班次。
- 建立主管—项目负责关系，所有主管接口按该关系过滤和鉴权。
- 项目主管在同一个 App 内完成项目 CRUD、GPS 半径（100/200/300/500 米）、人员、班组和排班管理。
- 测试人员切换、失效项目和历史打卡快照不被新归属改变。

### 阶段 4：可信打卡闭环

- App 获取相机、定位权限，展示定位精度和识别项目。
- 后端验证人员分配、项目状态、距离和打卡窗口。
- 使用 `requestId` 保证幂等。
- 照片上传可重试；区分待提交、上传中、已确认和失败状态。
- 围栏重叠时要求确认或阻止提交，不能静默选错项目。
- 围栏外允许提交，但 App 必须强提醒；记录进入异常待处理状态，在主管处理前不计工时和工数。
- 完成白班和夜班跨日端到端测试。

### 阶段 5：考勤计算与修正

- 计算迟到、早退、缺卡、正常、异常和工数。
- 所有异常默认不计入工时和工数，直到负责主管确认或修正。
- 生成每日结果，支持按规则版本重算。
- 项目主管修正前显示强提醒，修正原因必填；确认后通过调整记录补卡、作废和修正，不覆盖原始记录，也不需要审批流。
- 调整后只重算受影响人员和日期。
- 修正后的记录和报表必须显示“修正”标记。

### 阶段 6：月报与冻结

- 工人个人月报按常见工人考勤表展示日期、项目、班组、班次、工时、工数、异常和修正标记，不计算工资。
- 项目月报是考勤报表，按项目、月份、班组和人员汇总，不计算成本。
- 月报支持草稿、确认、冻结和版本追溯。
- 根据业务确认实现 Excel 导出或 App 内查看。

### 阶段 7：弱网、夜班与加班完善

- 实现离线队列或明确的失败申诉流程。
- 完善夜班高温季规则。
- 加班只计时不计工，处理重叠、审批和上限。
- 做照片重传、重复提交和跨月夜班专项测试。

## 六、第一版验收条件

1. 两个 App 的 Token 不能交叉访问接口。
2. 人员切换项目、班组或班次后，历史打卡仍保留打卡当时的归属。
3. 同一 `requestId` 重试不产生重复打卡。
4. 夜班跨日归属到正确考勤日和月份。
5. 原始打卡不能被直接覆盖，修正均可追溯。
6. 月报明细能追溯到原始打卡和调整记录。
7. 围栏外、精度不足和缺卡不会被算作正常出勤。

## 七、仍需业务确认

1. 临时换班、休息日、节假日和无排班打卡的处理方式。
2. 同一天跨项目工作时，工时和工数如何拆分。
3. 围栏外记录被主管确认有效时，是采用原打卡时间计算，还是必须填写修正后的时间。
4. 围栏默认半径（可选 100/200/300/500 米）和定位精度最低要求。
5. 是否允许离线打卡及最长补传时间。
6. 加班上限、审批和跨班次冲突规则。
7. 月报冻结人、冻结时间和 Excel 格式。
8. 同一项目是否允许多个主管同时负责，以及多个主管是否拥有相同权限。

## 八、环境与构建

- 本地：F5 前执行 `adb reverse tcp:8082 tcp:8082`。
- 测试包：`flutter build apk --flavor staging --dart-define=ENV=test`。
- 生产包：`flutter build apk --flavor prod --dart-define=ENV=prod`。
- release 签名缺失时构建必须失败。
