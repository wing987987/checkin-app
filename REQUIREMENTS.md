# 昭臣打卡 App 需求核对记录

> 来源：需求方手写稿照片（收到日期：2026-08-19）。
> 本文件区分“已确认需求”“待确认问题”“待提供资料”。在需求方确认前，不把推测当成正式业务规则。

## 〇、2026-08-19 已确认补充

1. 班次时间段和规则均在同一个 App 的管理端设置，包括白班、夜班、高温时段和加班规则。
2. 缺卡、迟到、早退只做异常标记；半天班按实际工时记录处理，不另设特殊审批流程。
3. 没有现成工资月报样表，个人报表参考常见工人考勤表设计，核心是考勤，不在本期计算工资。
4. 项目/班组报表是考勤报表，不是成本报表。
5. 项目 GPS 围栏半径由管理端设置，可选 100、200、300、500 米。
6. 照片水印采用常见相机水印样式，放在照片右上角，展示需要的打卡信息。
7. 管理员修正考勤不需要审批流程，但操作前必须强提醒、修正必须留痕，报表中对应记录要显示“修正”标记。
8. 所谓“后台”不是另做 Web 后台，而是当前打卡 App 内的项目主管管理端；项目主管登录后进入管理功能。
9. 项目主管只能管理自己负责的项目，不能管理其他主管负责的项目。
10. 班次按项目独立配置；每个项目可以有多种班次，每名工人直接放在当前班组和具体班次中，不设置分配有效期。主管调整时直接切换班组或班次。
11. 人员切换班组或班次后，只影响后续打卡；历史打卡继续使用打卡当时保存的项目、班组和班次信息。
12. 跨日夜班计入夜班开始当天，跨月时也按开始日期归属月份。
13. 缺卡、迟到、早退和围栏外等异常会影响最终工时和工数；主管确认或修正前不计入统计。
14. 主管可以修正异常；修正原因必填，修正前强提醒，修正记录在报表中体现。
15. 围栏外允许打卡，但提交前必须强提醒“会影响工时”；记录保存为异常，等待负责该项目的主管确认或修正。
16. 打卡 App 的 OSS 文件必须与项目物料管理 App 分目录存储。打卡照片统一使用 `checkin/projects/{项目ID}/workers/{工人ID}/clock/年/月/日/` 路径。

## 一、手写稿可明确识别的需求

### 1. 管理员权限

1. 可以创建项目，项目需要 GPS 定位。
2. 可以增加、删除人员。
3. 可以调整数据。
4. 可以把人员分配到班组。

手写稿中“可生成报表（按项目、按月）”及旁边“按项目/按月”有明显划线；后续已经确认报表功能保留，并由同一个 App 内的项目主管端查看，报表性质为考勤报表。

### 2. 工人操作流程

1. 系统自动识别工人排班，包括白班、夜班和加班。
2. 白班时间写明为：
   - 上午：06:30–11:30；
   - 下午：13:00–17:30；
   - 自动识别允许前后 5 分钟。
3. 夜班排班可能因高温调整，需要预留调整窗口，具体规则后续讨论。
4. 系统根据 GPS 自动识别项目。
5. 白班工人需要在上午、中午、下午、晚上四个节点拍照；四次形成闭环，计算为 1 个工。
6. 夜班工人需要在晚上、早上两个节点拍照；两次形成闭环，计算为 1 个工。
7. 加班只计算时间，不计算“工”。
8. 加班与白班/夜班可能重叠，涉及较多计算，需要单独明确规则。

### 3. 数据整理与报表

1. 每个工人每月单独生成报表，用于付工资。
2. 每个项目每月按班组生成报表。
3. 项目报表是按项目、月份和班组汇总的考勤报表，不是成本报表。
4. 所有打卡照片自动生成相机式水印，水印位于右上角。

## 二、与当前开发计划的核对结果

| 手写需求 | 当前计划状态 | 结论 |
|---|---|---|
| 管理员创建带 GPS 的项目 | 已包含 `ck_project` 和项目管理 | 一致 |
| 管理员增减人员 | 已包含 `ck_user` 和人员管理阶段 | 一致 |
| 管理员调整数据 | 已设计原始记录不可覆盖、调整记录留痕 | 一致，且便于追溯 |
| 人员分配班组和班次 | 工人保存当前项目、班组和班次；主管直接切换 | 一致，不设置有效期 |
| 自动识别白班/夜班/加班 | 已采用班次模板、检查点和每日排班 | 一致 |
| 白班 06:30–11:30、13:00–17:30 | 计划此前未固定到正式需求 | 现已记录为当前白班基础时间 |
| 前后 5 分钟自动识别 | 计划中已有时间窗口概念 | 一致，但边界含义待确认 |
| GPS 自动识别项目 | 已包含后端围栏判断 | 一致 |
| 白班四次闭环为 1 工 | 已支持四检查点模板 | 一致，异常情况待确认 |
| 夜班两次闭环为 1 工 | 已支持跨日两检查点模板 | 一致，具体时间待提供 |
| 加班只计时不计工 | 已放在加班完善阶段 | 一致，计算规则待提供 |
| 工人个人月报 | 已包含 | 一致 |
| 项目×月份×班组考勤报表 | 已包含 | 一致；不计算项目成本 |
| 照片自动水印 | 已包含 | 一致；采用右上角相机水印样式 |
| 管理后台 | 原计划表述容易理解成独立后台 | 已明确为同一个 App 内的项目主管端 |

## 三、需要需求方确认的问题

以下问题会影响数据库或考勤计算，应在对应阶段开发前得到答案。

### A. 班次与时间

1. 白班“前后 5 分钟”具体含义是什么：
   - 只允许在标准时间前后 5 分钟打卡；还是
   - 系统在前后 5 分钟内自动判断属于哪个检查点，但更早/更晚仍允许打卡并标记异常？
2. 四个白班拍照节点对应的标准时间是否就是 06:30、11:30、13:00、17:30？手写稿中的“上午、中午、下午、晚上”是否只是四个节点名称？
3. 夜班已经确认归属开始当天。
4. 班次已经确认按项目独立配置；每个项目可配置多种班次，并把工人分配到具体班次。
5. 临时换班、休息日和节假日如何设置？没有排班的工人能否打卡？

### B. 工数计算

1. 缺卡、迟到、早退和围栏外已确认影响最终工数；主管处理前不计入工时和工数。
2. 四次卡都打齐但时间不符合标准时，先标记异常且不计入统计，等待主管确认或修正。
3. 同一天上午和下午在不同项目工作，工时和工数如何拆分到两个项目考勤报表？
4. 夜班归属开始当天，因此 8 月 31 日开始、9 月 1 日结束的夜班计入 8 月。
5. 修正不需要审批，修正原因已经确认必填。

### C. 加班

1. 加班规则由管理端配置；需要确定首版可配置项：开始/结束时间、最小计量单位、取整方式和每日上限。
2. 加班是否需要项目主管提前排班？
3. 加班与白班/夜班重叠时，重叠时段是否扣除？
4. 加班是否有每日上限、取整规则和跨日规则？
5. 加班报表只展示时长，还是还需要计算加班工资？

### D. GPS 与项目识别

1. 每个项目是一个中心点加半径，还是需要多个坐标组成的施工区域？
2. 围栏半径已确定为 100、200、300、500 米可选；仍需确认默认选项和最低定位精度。
3. 同一位置命中多个项目时，是否只显示该工人当前有效分配中的项目？
4. 围栏外允许打卡，提交前强提醒，保存为异常且不计入统计，等待项目主管确认或修正。

### E. 报表与照片

1. 个人报表已明确为常见工人考勤表，不计算工资；建议列为日期、项目、班组、班次、首次/末次打卡、工时、工数、异常、修正标记。需求方需确认这些列是否足够。
2. 项目班组报表已明确为考勤报表；建议按人员汇总出勤天数、工数、正常工时、加班工时、异常次数和修正次数。
3. 报表是在 App 内查看，还是必须导出 Excel/PDF？
4. 已确认项目主管只能管理自己负责的项目。
5. 水印建议包含项目名称、姓名、日期时间、GPS 坐标/地址和打卡类型；需要确认最终字段。
6. 照片是否必须当场拍摄，是否允许从相册选择？

## 四、等待需求方提供的资料

| 编号 | 资料 | 用途 | 最迟需要时间 |
|---|---|---|---|
| M-01 | 无需固定时间表；提供一组期望的白班、夜班、高温班、加班初始默认值 | 初始化可配置班次 | 开发排班模块前 |
| M-02 | 同一天跨项目工作的工时和工数拆分规则 | 实现考勤计算 | 开发考勤计算前 |
| M-03 | 无现成样表；确认建议的个人考勤报表字段 | 确定个人报表格式 | 开发报表前 |
| M-04 | 确认建议的项目/班组考勤汇总字段 | 确定项目报表格式 | 开发报表前 |
| M-05 | 一份真实的人员、班组、项目关系示例 | 验证当前人员归属和切换操作 | 开发人员分配前 |
| M-06 | 项目定位资料示例（地址、坐标、允许范围） | 验证 GPS 项目识别 | 开发项目定位前 |
| M-07 | 确认右上角水印的最终字段 | 确定水印内容 | 开发拍照打卡前 |
| M-08 | 已确认：原因必填，负责主管只能修正自己项目的数据 | 确定修正权限 | 已提供 |

## 五、暂不阻塞当前开发的事项

以下内容尚未确认，但不影响账号鉴权及基础项目/人员 CRUD：

- 夜班、高温和加班的默认配置与精确计算规则；
- 个人和项目考勤报表具体列；
- 水印最终布局；
- 异常打卡审批流程。

在资料未到之前，可以继续开发账号、项目、班组、人员当前归属和班次规则的可配置框架；打卡计算和报表字段不得依靠猜测定稿。
